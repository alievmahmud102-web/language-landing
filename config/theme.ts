/**
 * Global visual theme configuration.
 * These values are injected into CSS variables in app/layout.tsx.
 */
export const themeConfig = {
  primaryColor: "oklch(0.45 0.18 264)",
  secondaryColor: "oklch(0.5 0.08 200)",
  font:
    'ui-sans-serif, system-ui, sans-serif, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji"',
  darkMode: false,
} as const;

